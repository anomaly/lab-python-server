apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python-lab-chart.fullname" . }}-test-connection"
  labels:
    {{- include "python-lab-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "python-lab-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
